# Logic PK 運用手順

## 1. 当日使う画面

- Mac: `/team/red`
- モニター1: `/team/blue`
- モニター2: `/`

## 2. 起動

```bash
npm run ble:stop
npm run ble:register
npm run dev:with-ble
```

## 2.1 結果を消すとき

試合結果だけを消して setup に戻す方法は 3 つあります。

1. RED 画面の終了後ボタン `試合をリセット`
2. 審判ボタンで `reset` に合わせて長押し
3. ターミナルで `npm run match:reset`

サーバ自体を止めたい場合は `npm run server:stop` を使います。

## 3. ブロック配置

1. 明るさブロック 1 個をゴール中央へ設置する
2. RED のボタン1とボタン2を配置する
3. BLUE のボタン1とボタン2を配置する
4. 審判ボタンを配置する

## 4. 設定登録

`npm run ble:register` で次を順に割り当てます。

1. `goalSensor`
2. `redCycle`
3. `redConfirm`
4. `blueCycle`
5. `blueConfirm`
6. `refereeControl`

必要なら、同じ物理ボタンを複数役割に割り当てて構いません。

## 5. setup 中の役割

- RED 画面
  - 設定編集
  - チーム名、選手名、基礎点、ボールのチーム別利用権の保存
  - 審判ボタン状態の確認
- BLUE 画面
  - 閲覧専用の準備確認
- 全体画面
  - 閲覧専用の準備確認

## 6. 試合前に RED 画面で決めるもの

- チーム名
- 選手名
- 各選手の基礎点
- ボール名
- 各ボールの RED / BLUE 利用権
  - 各チーム `1` または `2`

保存後、各チームの基礎点合計が `ceil(人数 × 2.5)` になっていることを確認します。

## 7. 審判ボタンで決めるもの

試合前は審判ボタンで次を操作できます。

- 制限時間
- 先攻チーム
- 成功判定閾値
- 自動キャリブレーション
- 試合開始
- リセット

操作:

- 単押し: 次の項目へ
- ダブルクリック: 前の項目へ
- 長押し: 現在項目を実行

## 8. 自動キャリブレーション

1. ゴールを空にする
2. 審判ボタンで `自動キャリブレーション` へ移動する
3. 長押しする

結果:

- `minRaw = 0`
- `emptyRaw = latestRaw`
- `triggerThreshold = そのまま`

## 9. 1ターンの流れ

### 9.1 投げる側

- ボタン1: 投球者候補を進める
- ボタン2: 投球者確定
- ボタン1: 距離候補を進める
- ボタン2: 距離確定

### 9.2 守る側

- ボタン1: ボール候補を進める
- ボタン2: ボール確定
- ボタン1: 追加得点の向き候補を進める
- ボタン2: 追加得点の向きを確定
- 各向きの残り権利が `0` なら、その向きは選べない
- ボール利用権は RED / BLUE で別管理され、共有しない

### 9.4 表示制限

- RED / BLUE 画面では、両チームがそのターンの全項目を決めるまでは、相手チームが決めた内容を表示しない
- 全体画面 `/` でも、両チームの選択がそろうまではターン詳細を表示しない
- 追加得点の向きは、両チームの選択完了後も、成功 / 失敗 / 無効が確定するまで表示しない
- 進行中は、全体画面とチーム画面で詳細なボタン入力ログを表示しない

### 9.3 審判

- 両チーム確定後、審判ボタン長押しでターン開始
- 投球中、必要なら審判ボタン長押しでレビューへ
- レビューでは審判ボタンで結果を確定

## 10. 試合開始から終了まで

1. RED 画面で静的設定を保存する
2. 審判ボタンで制限時間と先攻を決める
3. 自動キャリブレーションを行う
4. 審判ボタンで `試合開始`
5. RED 画面は RED チーム画面へ切り替わる
6. BLUE 画面は BLUE チーム画面へ切り替わる
7. 全体画面はメイン画面へ切り替わる
8. 試合終了後、結果は全体画面 `/` にそのまま表示される

## 11. トラブルシュート

### 11.1 `No MESH blocks found`

```bash
npm run ble:stop
npm run ble:discover
```

確認点:

- 他端末や MESH アプリが接続していないか
- ブロックの電源が入っているか
- Mac の Bluetooth が有効か

### 11.2 センサーは動くがボタンが反応しない

1. `npm run ble:register` をやり直す
2. `mesh-ble.config.json` に 5 つのボタンIDが入っているか確認する
3. `npm run dev:with-ble` のログで各ボタンが `ready` になっているか確認する

### 11.3 setup 画面が想定どおりに出ない

- RED は `/team/red`
- BLUE は `/team/blue`
- 全体は `/`

この割り当てを逆にすると、setup 中の見え方が違います。

### 11.4 サーバを止めたはずなのに前回結果が残って見える

- `npm run dev:with-ble` は、既に `3000` 番で動いているサーバを再利用します
- そのため、前回の Node サーバが残っていると状態もそのままです

確認と停止:

```bash
lsof -nP -iTCP:3000 -sTCP:LISTEN
npm run server:stop
```

試合状態だけ消したいなら、停止ではなく `npm run match:reset` を使ってください。
